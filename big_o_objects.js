
/* Cell ID: a212vddh*/
let instructor = {
  firstName:"Kelly",
  isInstructor:true,
  favoriteNumbers: [1,2,3,4]
}

// show(Object.keys(instructor)) // O(n)
// show(Object.entries(instructor)) // O(n)
// show(instructor.hasOwnProperty("firstName")) // O(1)

/* Cell ID: x37q6xno*/
//Arrays

let names = ['Michael','Melissa','Andrea'];
let values = [true,{},[],2,'awesome'];

/**
 * Insertion..depends
 * Removal...depends
 * Access..O(1)
 * Searching - O(n)
 */

/**
 * push - O(1)
 * pop - O(1)
 * shift - O(n)
 * unshift - O(n)
 * concat - O(n)
 * slice - O(n)
 * splice - O(n)
 * sort - O(n * log N)
 * forEach/map/filter/reduce/etc. - O(n)
 */
